# AI Plagiarism Checker (v1.2.0)

Простий інструмент для перевірки схожості документів на основі алгоритмів машинного навчання (TF-IDF та косинусна міра).

## Ключові можливості:
  **Smart Analysis**: Використання TF-IDF для аналізу контексту та лексичного складу документів.
  
  **Формати**: Підтримка файлів .txt та .docx.
  
  **Privacy**: Повна локальна обробка даних без використання хмарних сервісів.
  
  **Logging**: Детальний журнал подій та помилок у файлі `app.log` з підтримкою UTF-8.

## Що нового у гілці task2 (+fix):
  **Експорт у JSON**: Додано новий модуль для збереження результатів аналізу у структурований файл `report.json`.
  
  **Оновлений GUI**: Додано інтерактивну кнопку для миттєвого експорту звіту.
  
  **Валідація**: Покращено обробку виняткових ситуацій (порожні значення, помилки доступу до файлів).
  
  **Стабільність**: Виправлено помилки імпорту бібліотеки логування та покращено сумісність версій.

## Структура проєкту (module_2):
  `main.py` — Графічний інтерфейс (GUI) та логіка експорту.
  
  
  `test_logic.py` — Математичне ядро та модульні тести (Unit-тести).
  
  
  `config.json` — Зовнішні налаштування версії ПЗ та параметрів інтерфейсу.
  
  
  `README.md` — Короткий опис реалізованих функцій та інструкція.

## Швидкий запуск:
1. **Встанови бібліотеки**: 
   `pip install scikit-learn python-docx`
2. **Запусти програму**: 
   `python main.py`
3. **Проведи тестування**: 
   `python test_logic.py`

## Гілка розробки:
  Проєкт реалізовано у гілці **task2** репозиторію **module_2**.
